extends ../layout/index

block contenido
    div(class="py-10")
        h1(class="text-4xl my-10 font-extrabold text-center") Bienes
            span(class="font-normal") Raíces
        h2(class="text-center text-2xl font-extrabold")= pagina

        if errores 
            div(class="max-w-md mx-auto my-10")
                each error in errores
                    p(class="bg-red-600 text-white uppercase text-xs text-center p-2 mb-1 font-bold rounded-lg")=error.msg

        div(class="mt-8 mx-auto max-w-md")
            div(class="bg-white py-8 px-4 shadow rounded-lg")
                form(class="space-y-5" method="POST" action="/auth/registro")
                    input(type="hidden" name="_csrf" value=csrfToken)
                    div
                        label(class="block text-sm uppercase text-gray-500 mb-2 font-bold" for="nombre")  Tu Nombre
                        input(id="nombre" class="w-full px-3 py-2 border border-gray-300 rounded-lg placeholder-gray-400" placeholder="Ej. Hernán" type="text" name="nombre" value=usuario ? usuario.nombre: "")

                    div
                        label(class="block text-sm uppercase text-gray-500 mb-2 font-bold" for="email")  Email de Registro
                        input(id="email" class="w-full px-3 py-2 border border-gray-300 rounded-lg placeholder-gray-400" placeholder="Ej. correo@correo.com" type="email" name="email" value=usuario ? usuario.email : "")

                    div
                        label(class="block text-sm uppercase text-gray-500 mb-2 font-bold" for="password") Password
                        input(id="password" class="w-full px-3 py-2 border border-gray-300 rounded-lg placeholder-gray-400" placeholder="Escribe tu Password" type="password" name="password")

                    div
                        label(class="block text-sm uppercase text-gray-500 mb-2 font-bold" for="repetir_password") Repetir Password
                        input(id="repetir_password" class="w-full px-3 py-2 border border-gray-300 rounded-lg placeholder-gray-400" placeholder="Repite tu Password" type="password" name="repetir_password")

                    div(class="flex items-center justify-between")
                        a(href="/auth/login" class="text-gray-500 text-xs") ¿Ya tienes una Cuenta? Inicia Sesión
                        a(href="/auth/olvide-password" class="text-gray-500 text-xs") Olvidé mi Password 

                    input(type="submit" value="Crear Cuenta" class="w-full bg-indigo-600 hover:bg-indigo-800 text-white font-bold py-3 rounded-lg cursor-pointer")