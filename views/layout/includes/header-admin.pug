header(class="bg-indigo-600 p-4")
    div(class="container mx-auto flex justify-between items-center")
        a(href="/mis-propiedades") 
            h1(class="text-2xl text-white font-extrabold text-center") Bienes
                span(class="font-normal") Raíces

        nav(class="my-5 text-sm md:flex md:gap-3 md:items-center font-bold text-white hidden")
            a(href="/mis-propiedades") Mis Propiedades 
            a(href="") Mi Perfil 
            form(action="/auth/cerrar-sesion" method="POST")
                input(type="hidden" name="_csrf" value=csrfToken) 
                input(type="submit" value="Cerrar Sesión" class="bg-indigo-800 py-2 px-10 rounded-lg cursor-pointer")